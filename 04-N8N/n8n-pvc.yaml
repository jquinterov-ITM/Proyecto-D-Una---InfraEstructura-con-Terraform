apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  # Si usas EFS, usa la StorageClass correcta para tu cluster (por ejemplo: efs-sc)
  # storageClassName: efs-sc
